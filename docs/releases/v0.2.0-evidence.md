# v0.2.0 candidate evidence

This is a development evidence record, not publication evidence. The acceptance
criteria were recorded in [V0_2_PLAN.md](../V0_2_PLAN.md) before implementation.

## Baseline and method

- Public baseline: tag v0.1.0, commit `7107aa049f5ffbefaa9e1fcdf1552f974c967185`.
- Candidate branch: `codex/v0.2.0-small-and-useful`.
- Local environment: Windows 11 build 26100, Python 3.14.3, Playwright 1.63.0,
  Pydantic 2.13.4. No runtime dependency was added or minimum version increased.
- Tests were reviewed separately: the two unsupported-future-plan tests now use
  version 4 because version 3 is implemented. Only release-specific version/file
  expectations advance to 0.2.0. Existing v2 digest assertions and goldens remain.

## Startup measurements

Command: `python scripts/benchmark_cli.py --samples 15`. The script exports the
released source to a temporary directory and alternates baseline/current fresh
Python subprocesses using the same interpreter and installed dependencies. Two
warmups per variant/command are excluded. Measurements include interpreter startup;
they are **not** browser execution benchmarks or cold-disk-cache measurements.

| Command | v0.1.0 median | Candidate median | Reduction |
| --- | --- | --- | --- |
| `--version` | 419.958 ms | 119.672 ms | 71.50% |
| `--help` | 415.624 ms | 129.615 ms | 68.81% |

Raw samples are in [v0.2.0-startup.json](v0.2.0-startup.json). Performance depends
on machine load and platform; CI tests import boundaries, not timing thresholds.
Help/version do not load Pydantic/Playwright. Validate/inspect do not load Playwright.

## Completed local gates

Runtime implementation commit: `71cf5d7174fe2eb9a20e72472c442a93d9d6affb`.
This includes the application-startup interrupt receipt fix.

- `pytest -q`: **296 passed, 16 skipped**, 107.49 seconds on the local Windows
  Python 3.14.3 environment. Existing skips cover unavailable local Docker,
  POSIX-only signal/process behavior, and the host's missing file-symlink capability.
- `ruff check .`: passed. `mypy src tests`: passed (45 source files).
- Matching Playwright-managed Chromium headless shell v1243 was downloaded and used.
  Earlier missing-browser UNKNOWN outcomes were not counted as passes.
- Wheel and sdist built; distribution content/metadata checks passed.
- Both artifacts were separately installed in clean, out-of-checkout virtual
  environments. Version/help, installed import/entrypoint identity, absence of old
  namespaces, `pip check`, and offline Plan v3 validation passed.
- Both installed artifacts completed real Chromium greeting verification and all
  three expense variants. Every resulting bundle passed `inspect`.

| Expense mode | Input validation | Exact total/form state | Persistence | Exit |
| --- | --- | --- | --- | --- |
| `none` | PASS | PASS | PASS | 0 |
| `wrong-total` | PASS | FAIL | PASS | 1 |
| `fake-save` | PASS | PASS | FAIL | 1 |

Candidate artifacts from CI run 35721682822 were downloaded, content-checked, and
smoked on Windows. All 17 wheel runtime source files match commit 71cf5d7.
SHA-256 values below identify those candidate bytes, not a public release:

```text
ccca9844d8bdaad3c5e93088b44b4b0a83edf11fb0a23f79d5e759ee37954a58  donewitness-0.2.0-py3-none-any.whl
4ac49af0f7467fdc6a4f66d09b47be4777e466f7d0a84caec23c283b13777a26  donewitness-0.2.0.tar.gz
```

## Hosted verification and release boundary

[Run 35721682822](https://github.com/Drippinblood333/DoneWitness/actions/runs/35721682822)
passed all **8/8 jobs** for implementation commit 71cf5d7:

| Gate | Observed result |
| --- | --- |
| Linux Python 3.12 / 3.13 / 3.14 | 312 passed each; Docker required |
| Windows Python 3.12 / 3.13 / 3.14 | 297 passed, 15 platform skips each |
| Ruff / mypy in all six quality jobs | Passed; 45 source files checked |
| Linux distribution | Wheel and sdist clean install + browser/fault smoke passed |
| Windows distribution | Wheel browser/fault smoke + sdist CLI smoke passed |


[PR #14 checks](https://github.com/Drippinblood333/DoneWitness/pull/14/checks)
are the authoritative live status for the latest candidate. Require all eight jobs
on the exact reviewed head: Linux/Windows Python 3.12, 3.13, 3.14 plus two
artifact jobs. Linux quality jobs require actual Docker execution. Local Windows
results alone do not establish those platform qualifications.

The initial CI exposed wrapped Playwright selector errors being reported as FAIL.
The runtime now diagnoses failed v3 assertions through public APIs and reports
unusable selectors as UNKNOWN; original verdict expectations were kept. A too-short
value-diagnostic timeout was also corrected to use the configured bounded timeout.

Run [35720283054](https://github.com/Drippinblood333/DoneWitness/actions/runs/35720283054)
passed seven jobs, but Windows/Python 3.12 had an expense smoke UNKNOWN without
sufficient receipt diagnostics in its log. This is retained as an unsuccessful
attempt, not described as a fixed root cause. The test now includes the persisted
receipt on failure without changing the required verdict or increasing timeouts.
Use the latest head checks for subsequent qualification; do not infer success from
this historical run.

The later pre-fix run 35721097572 exposed an early Docker-startup interrupt escaping
before receipt finalization. That path is now caught and covered by a deterministic
regression plus the unchanged real Docker interrupt test. A Windows assertion UNKNOWN
in that run was not reproduced in the subsequent full matrix; no timeout or expected
verdict in its test was relaxed. This record does not claim the absence of all transient
infrastructure failures.

## Acceptance audit

| Frozen requirement | Evidence |
| --- | --- |
| Legacy formats, verdicts, integrity | Existing v2 digest and receipt goldens retained; full suite above |
| Four strict v3 assertions and private diagnostics | Schema, real Chromium, tampered-evidence and no-start-on-invalid-input tests |
| Correct/wrong-total/fake-save discrimination | Identical plan, exact per-criterion verdicts in table above, installed-artifact smoke |
| Offline validation and concise entry point | CLI validation/import-boundary tests; README with intact local links |
| Measured startup reduction | Raw alternating samples and benchmark procedure above; no browser-speed extrapolation |
| Tests, packaging, upgrade/release materials | Full local/matrix gates, verified candidate bytes, changelog, v0.2 notes and runbook |

No v0.2.0 public tag, PyPI upload, GitHub Release, or merge is claimed by this record.
Those require the separate reviewed release ceremony in [RELEASE.md](../RELEASE.md).

## Interpretation limits

The new expense app is a maintained browser-localStorage fixture with controlled
wrong-total/fake-save faults, not an externally adopted project or database test.
The observed verdicts establish detection of these two specific injected faults only.
No speed claim applies to browser procedures, and no integrity check was removed
to obtain the CLI startup improvement.
